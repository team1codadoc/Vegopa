import React from "react";
import styled from "styled-components";

const Container = styled.div`
  position: relative;
`;

const Restaurant = () => {
  return <Container>파티 생성</Container>;
};

export default Restaurant;
